[project]
name = "crew_llm_analyst"
version = "0.1.0"
description = "crew_llm_analyst using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.13.0"
]

[project.scripts]
crew_llm_analyst = "crew_llm_analyst.main:run"
run_crew = "crew_llm_analyst.main:run"
train = "crew_llm_analyst.main:train"
replay = "crew_llm_analyst.main:replay"
test = "crew_llm_analyst.main:test"
run_with_trigger = "crew_llm_analyst.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
